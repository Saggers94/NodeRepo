doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{docTitle}
        link(rel="stylesheet", href="/css/main.css")
        link(rel="stylesheet", href="/css/product.css")
    body 
        //- Indentation is important to let you know how the tags forms inside each other
        //- you just have to type the tag you want to use with the class you want to use it with, example : "header.main-header"
        //- to output the variable in pug you have to use "#{product}" or "#{product.title}"
        header.main-header
            nav.main-header__nav
                ul.main-header__item-list
                    li.main-header__item
                        a.active(href="/") Shop 
                    li.main-header__item
                        a(href="/admin/add-product") Add Product 
        main 
            //- Checking if the products has atleast one element to iterate through
            if prods.length > 0
                .grid
                    //- the below is a loop in pug for iteration
                    each product in prods
                        article.card.product-item
                            header.card__header
                                h1.product__title #{product.title}
                            .card__image
                                img(src="https://cdn.pixabay.com/photo/2016/03/31/20/51/book-1296045_960_720.png", alt="A Book") 
                        
                            .card__content
                                h2.product__price $19.99
                                p.product__description A very interesting book about so many even more interesting things!
                            
                            .card__actions
                                button.btn Add to Cart
            else 
                h1 No Products